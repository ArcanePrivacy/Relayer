## Reverse-proxy / TLS settings (only for the server container)
VIRTUAL_HOST=example.duckdns.org
LETSENCRYPT_HOST=example.duckdns.org

## Optional but recommended: explicitly tell nginx-proxy
## which internal port to send traffic to.
VIRTUAL_PORT=8000

