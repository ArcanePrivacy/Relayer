map $ssl_preread_server_name $name {
    yourdomain.com arcane_solana;

    default arcane_solana;
}

upstream arcane_mainnet {
    server 127.0.0.1:4380;
}

server {
    listen 0.0.0.0:443;
    proxy_pass $name;
    ssl_preread on;
}
